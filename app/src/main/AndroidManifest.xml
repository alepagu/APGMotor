<?xml version="1.0" encoding="utf-8"?> <!-- Declaración de la versión y el lenguaje que se va a implementar -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos para poder usar la base de datos con internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Describimos las reglas que sigue este programa, y personalizamos el tema -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!-- Se añaden todas las clases y actividades necesarias para el desarrollo de la aplicación APGMotor -->
        <activity
            android:name=".Fallostestigos_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Adaptadortestigos_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Vertestigos_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Preguntasfrecuentes_apgmotor"
            android:exported="false" />
        <activity
            android:name=".vergastos_apgmotor"
            android:exported="false" />
        <activity
            android:name=".ingresargastos_apgmotor"
            android:exported="false" />
        <activity
            android:name=".planificarfecha_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Cambiarcontrasenna_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Configuracion_apgmotor"
            android:exported="false" />
        <activity
            android:name=".nuevovehiculo_apgmotor"
            android:exported="false" />
        <activity
            android:name=".paginaprincipal_apgmotor"
            android:exported="false" />
        <activity
            android:name=".Registro_apgmotor"
            android:exported="false" />
        <!-- Indicamos que ventana se nos muestra primero y le añadimos una ventana de carag a la aplicación -->
        <activity
            android:name=".Login_apgmotor"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>